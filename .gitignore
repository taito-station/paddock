/target
/data/*.db
/data/*.db-journal
/data/*.db-wal
/data/*.db-shm
# seed-db.sh / reset-db.sh が退避する .bak（#120）
/data/*.bak-*
/pdfs/**/*.pdf
# JRA 著作物の PDF はリポジトリに含めない（テストはローカル優先 / 結果PDFは不在時DL）
/samples/*.pdf
# DL fallback のキャッシュ書き込み中間ファイル
/samples/*.tmp
.DS_Store
.env

# create-pr スキルの一時 PR 本文 (コミット対象外)
docs/temp/

# Claude Code のエージェント用一時 worktree
.claude/worktrees/

# Python バイトコードキャッシュ（scripts/ のハーネス用）
__pycache__/
*.pyc

# refresh_ev.sh 等のランタイムログ（WORKDIR をリポジトリ内に設定した場合の安全網）
logs/

# Serena MCP のローカルキャッシュ・ローカル上書き設定（バージョン管理対象外）
.serena/

# mdq (Markdown Query) の索引 DB と venv（セッション毎に再ビルド前提・共有しない）
.mdq/
tools/mdq/.venv/

# late_money_probe.py の netkeiba 結果ページローカルキャッシュ（#315）
scripts/predict-check/.cache_nk_results/
