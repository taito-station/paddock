/target
/data/*.db
/data/*.db-journal
/data/*.db-wal
/data/*.db-shm
/pdfs/**/*.pdf
# JRA 著作物の PDF はリポジトリに含めない（テストはローカル優先 / 結果PDFは不在時DL）
/samples/*.pdf
# DL fallback のキャッシュ書き込み中間ファイル
/samples/*.tmp
.DS_Store
.env

# create-pr スキルの一時 PR 本文 (コミット対象外)
docs/temp/

# Claude Code のエージェント用一時 worktree
.claude/worktrees/
