[package]
name = "pdf-ocr"
version = "0.1.0"
edition = "2024"

[dependencies]
tempfile  = { workspace = true }
thiserror = { workspace = true }
tracing   = { workspace = true }

[dev-dependencies]
# 不在時に JRA からサンプル PDF を取得する fallback 用（本体 fetcher と同じ ureq）
ureq = { workspace = true }
